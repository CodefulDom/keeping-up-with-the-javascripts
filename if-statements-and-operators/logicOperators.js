const accountBalance = 124534.54
const costOfItem = 50
const canSpendMoney = true

if (accountBalance >= costOfItem && canSpendMoney) {
	console.log('Have fun spending that money bruh!!!')
} else {
	console.log(`No, no spending!!!!!`)
}
